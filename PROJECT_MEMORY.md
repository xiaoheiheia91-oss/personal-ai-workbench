# Personal AI OS Project Memory

本文档记录 Personal AI OS 的长期项目基线、已确认约束与开发原则。后续开发应以此为上下文，但新的明确决策可以更新本文件。

## 1. 项目定位

### 产品目标

Personal AI OS 是一个为个人长期使用而设计的 AI 工作台。它通过连接目标、任务、捕获、知识、决策和复盘，帮助用户明确当前重点、把想法转化为行动，并持续沉淀可复用的个人知识。

当前核心闭环是：

```text
今日主线 → 快速捕获 → 任务或笔记 → 完成与整理 → 每日回顾 → 长期沉淀
```

### 用户场景

- 每天打开系统后，快速知道今天最重要的事情。
- 随时记录临时想法，避免信息丢失。
- 将捕获内容整理为任务或结构化笔记。
- 管理今日任务、待处理任务和已完成任务。
- 在一天结束时查看完成情况并记录明日主线。
- 搜索、筛选和复用过去积累的知识。
- 未来让 AI 在用户授权的结构化上下文内辅助分析，但不擅自执行外部操作。

### 长期愿景

长期目标是形成一个属于个人、数据可控、能够持续演进的“第二大脑”：

```text
目标 → 项目 → 任务 → 知识 → 决策 → 创作 → 复盘 → 长期记忆
```

它不是商业 SaaS，也不以多用户、组织权限或企业流程为优先目标。产品应保持低维护成本、清晰的数据边界和渐进式演进能力。

## 2. 当前版本基线

- 当前版本：Alpha 3
- Git commit：`c0e974e`
- Commit 信息：`Implement alpha.3 knowledge base and context layer`

### 已完成功能

- 今日主线保存与展示。
- 快速捕获想法，支持分类和来源。
- 捕获转任务，并保留来源关系。
- 捕获转笔记，并在保存前编辑标题、正文、标签、目标和项目。
- 今日任务、待处理任务和已完成任务分组。
- 任务标题、优先级、截止日期、状态和今日标记编辑。
- 任务完成记录和首页统计。
- 每日回顾数据展示与明日主线保存。
- 结构化知识库：笔记创建、编辑、删除、归档、搜索和标签筛选。
- 首页最近笔记、今日新增笔记和待整理捕获统计。
- 面向未来 AI 参谋的只读上下文接口 `getAIContext()`。
- 桌面端与移动端基础适配。
- 旧版本数据迁移和缺失字段安全补齐。

### Alpha 1 至 Alpha 3 演进

#### Alpha 1：捕获到任务的最小闭环

- Commit：`76cf812 Implement alpha capture to task loop`
- 建立 `storage.js`，统一封装 `localStorage`。
- 实现“输入想法 → 保存到捕获箱 → 转换为任务 → 标记完成 → 首页反馈”。
- 为 Capture 预留 `source`，为 Task 预留 `dueDate` 和来源关系。

#### Alpha 2：每日个人驾驶舱

- Commit：`f65d7e0 Implement alpha.2 daily cockpit and task management`
- 增加今日任务、待处理任务和已完成任务分组。
- 增加任务编辑、截止日期、优先级、状态和今日标记。
- 优化捕获转任务流程，转换前可编辑任务信息。
- 增加每日回顾、今日完成情况和明日主线。
- schema 升级为 v2，并保持旧数据兼容。

#### Alpha 3：知识沉淀与上下文层

- Commit：`c0e974e Implement alpha.3 knowledge base and context layer`
- schema 升级为 v3。
- 增加 Note v3、旧笔记迁移和完整笔记 CRUD。
- 增加捕获转笔记、笔记归档、搜索和标签筛选。
- 增加首页知识沉淀统计与最近笔记。
- 增加严格只读的 `getAIContext()`，为未来 AI 能力提供稳定的数据投影。

## 3. 技术架构

### 技术栈

- HTML
- CSS
- 原生 JavaScript
- 浏览器 `localStorage`
- Vercel 静态部署
- 无构建步骤
- 无运行时服务器依赖

### 数据存储方式

- 所有生产数据存储在浏览器 `localStorage`。
- `storage.js` 是数据访问、归一化、迁移和上下文投影的唯一入口。
- UI 逻辑不应绕过 `storage.js` 直接修改持久化数据。
- 当前数据不会自动跨设备同步。
- 清除浏览器数据可能导致本地数据丢失，因此未来应优先考虑可验证的数据导出与恢复能力。

### 文件结构原则

- `index.html`：页面语义结构和表单、区域、对话框等界面骨架。
- `style.css`：统一视觉语言、组件样式和移动端适配。
- `app.js`：DOM 绑定、用户交互、页面状态和渲染逻辑。
- `storage.js`：数据模型、迁移、CRUD 和只读上下文接口。
- `tests/`：手动验收清单和后续测试资产。
- `README.md`：面向使用者的当前版本说明。
- `PROJECT_MEMORY.md`：面向后续开发者和 AI 助手的长期项目基线。

新增能力应优先扩展现有边界。只有当文件职责明显失控时，才进行有目标的模块拆分；不做与当前功能无关的大面积重构。

## 4. 数据模型原则

### Schema 演进规则

- 根状态必须包含显式 `schemaVersion`。
- 每次结构变更必须定义从旧版本到新版本的迁移路径。
- 迁移应是确定性的，多次归一化同一份数据不应持续产生变化。
- 新字段必须提供安全默认值，避免 `undefined` 导致渲染或业务逻辑崩溃。
- 数据模型升级应先在 `storage.js` 完成，再接入 UI。
- 每次 schema 升级必须验证旧数据读取、刷新持久化和核心 CRUD。

### 向后兼容原则

- 旧 Capture、Task、Note、Decision、Goal 和 DailyReview 必须保持可读取。
- 已存在的 ID、创建时间和来源关系不得无故重写。
- 新字段缺失时进行补齐，不因字段不完整而丢弃整个对象。
- 旧 API 如果仍被现有页面使用，应保留兼容行为或先完成调用方迁移。
- 无法识别的可保留数据应优先保留，而不是静默删除。

### localStorage 数据保护原则

- 不在迁移过程中删除原始业务内容。
- 不因 JSON 解析失败覆盖现有原始值。
- 写入前先完成归一化和必要校验。
- `getAIContext()` 等查询方法不得产生持久化副作用。
- 删除、清空、批量覆盖和导入操作必须有明确的用户确认和恢复策略。
- 在引入复杂迁移或导入能力前，应先设计备份、导出和回滚机制。

## 5. AI 上下文设计

### `getAIContext()`

`getAIContext(options)` 是未来 AI 参谋读取项目数据的统一入口。它当前只负责构造上下文，不调用模型、不发送网络请求、不执行操作。

设计要求：

- 严格只读，调用前后 `localStorage` 内容保持不变。
- 返回稳定且可序列化的 JSON。
- 不暴露 `localStorage` 原始字符串或内部存储细节。
- 根据 scope 返回最小必要上下文。
- 默认过滤不应参与当前分析的归档内容。
- 返回 `schemaVersion`、`generatedAt` 和 `scope`，便于未来调用方校验。

### Scope 含义

- `dashboard`：首页驾驶舱所需的摘要，包括当前主线、今日任务、待处理任务、近期捕获、近期笔记、决策和回顾摘要。
- `today`：与当天相关的主线、今日任务、今日完成、今日捕获和当日回顾。
- `capture`：指定捕获或待整理捕获，以及其分类、来源和已建立的转换关系。
- `task`：指定任务或任务集合，以及状态、优先级、日期和来源上下文。
- `note`：指定笔记或笔记集合，以及标签、来源和目标、项目等关联信息。
- `decision`：指定决策或决策集合，包括问题、选择、原因、风险和结果。
- `review`：每日回顾数据，包括完成情况、未完成事项和后续主线。
- `all`：经过显式投影和过滤的完整上下文，用于需要跨模块分析的场景；不等同于暴露原始存储结构。

未来接入 AI 时，应默认选择最小 scope，并让用户知道 AI 将使用哪些数据。任何由 AI 产生的任务、笔记、决策或长期记忆，都应经过用户确认后保存。

## 6. 开发限制

除非经过明确的重新评估和用户确认，禁止引入：

- React
- Next.js
- 后端
- 数据库
- 第三方依赖

同时保持以下边界：

- 不提前实现完整 AI Agent。
- 不让 AI 自动执行外部操作。
- 不引入登录、多用户、组织权限或商业 SaaS 能力。
- 不为未来可能需要的功能提前增加复杂基础设施。
- 不破坏 Vercel 纯静态部署兼容性。

若现有架构确实无法支持下一阶段目标，应先提交技术评估，说明迁移收益、成本、数据保护方案和回滚路径，再决定是否解除限制。

## 7. 下一阶段规划

以下内容仅记录候选方向，不代表已确认需求，不应提前实现。

### Alpha 4 候选功能

- 数据导出、导入和恢复，降低 `localStorage` 单点丢失风险。
- 捕获箱状态筛选、批量整理和来源追踪增强。
- 任务排序、逾期提示和更完整的回顾历史。
- 笔记详情体验、归档管理和跨对象关联展示。
- 决策记录的创建、编辑、状态和结果复盘能力。

### AI 上下文增强

- 为各 scope 增加明确的筛选参数和对象 ID 校验。
- 控制上下文长度、时间范围和返回数量。
- 提供上下文预览，让用户在调用 AI 前确认数据范围。
- 为 AI 输出定义结构化建议格式，但仍由用户确认后写入。

### 决策系统

- 记录决策问题、候选方案、最终选择、原因、风险和预期结果。
- 支持复查日期、实际结果和决策复盘。
- 将决策关联到目标、任务、笔记和捕获来源。
- 未来允许 AI 辅助识别假设与风险，但不替用户做决定。

### 个人知识网络

- 展示 Note、Capture、Task、Goal 和 Decision 之间的显式关系。
- 优先使用用户确认的结构化关联，不先引入复杂自动知识图谱。
- 支持从目标或项目回溯相关知识、行动和决策。
- 在数据量和检索需求被验证前，不引入向量数据库或语义搜索基础设施。

## 8. Git 提交规范

每个开发阶段必须：

- 小步提交，每次提交只包含一个清晰、可验证的目标。
- 保持可回滚，不把无关重构、临时产物和功能修改混入同一提交。
- 提交前运行 `git diff --check`。
- 检查 `git status`，明确区分当前改动与既有未跟踪文件。
- 根据影响范围完成浏览器静态测试、数据迁移测试或手动验收。
- 更新相关 README、验收清单和项目记忆。
- 使用能够描述结果的提交信息。

推荐的阶段提交格式：

```text
Implement alpha.N <capability>
```

修复和文档提交应使用同样明确、单一目的的描述。任何临时测试文件、截图、浏览器配置或 `work/` 内容都不应进入生产提交，除非它们被明确纳入项目资产。
