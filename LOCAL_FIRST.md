# Personal AI OS Local First 本地运行指南

本文档说明如何在没有 VPN、没有外部网络和没有 AI 服务的情况下使用 Personal AI OS Alpha 3.5.2。

## 1. Local First 的含义

当前版本的核心功能全部在浏览器中运行：

- 今日主线和今日任务。
- 捕获箱以及捕获转任务、转笔记。
- 任务状态和历史记录。
- 笔记、知识库、标签与搜索。
- 决策、每日复盘和复盘历史。
- 个人记忆中心和全局搜索。
- 完整 Schema v3 JSON 备份。

页面脚本不调用模型 API，也不发送业务数据。AI 参谋仍是占位能力；AI 不可用不会影响任何核心功能。

## 2. 运行模式选择

| 模式 | 适合场景 | 外部网络 | 数据保存位置 |
| --- | --- | --- | --- |
| `file://` | 单台电脑快速离线使用 | 不需要 | 电脑当前浏览器的本地文件 origin |
| 本地 HTTP | 电脑固定使用，或手机通过 Wi-Fi 访问 | 不需要，只需要局域网 | 每台设备当前浏览器的 HTTP origin |
| Vercel | 在外部网络方便访问 | 页面加载需要互联网 | 每台设备当前浏览器的 Vercel HTTPS origin |

三种模式可以共存，但不会自动共享数据。

## 3. 直接打开 `index.html`

1. 保持以下运行文件在同一目录：

   - `index.html`
   - `style.css`
   - `storage.js`
   - `memory.js`
   - `app.js`

2. 双击 `index.html`，或将其拖入桌面浏览器。
3. 创建一条测试记录并刷新页面，确认记录仍然存在。
4. 正式使用前导出一次 JSON，确认浏览器允许文件下载。

此模式不需要启动服务器，断网后也能打开。浏览器规范没有为每种 `file://` 场景保证统一的存储行为，因此不要在不同浏览器之间混用同一个本地文件入口。长期数据建议使用固定的本地 HTTP 地址，并定期备份。

## 4. 在电脑上启动本地 HTTP

本节以 Python 3 自带的静态服务器为例。Python 只负责把五个静态文件发送给局域网浏览器，不参与业务运行，也不会接收应用数据。先运行 `py --version` 或 `python --version`；如果两者都不可用，需要预先准备 Python 3 或等效的可信静态服务器。

### 快速方式

在可信的家庭或个人局域网中，可以从项目目录运行：

```powershell
ipconfig
py --version
$lanAddress = "192.168.2.152" # 替换为电脑实际的 WLAN IPv4 地址
py -m http.server 8000 --bind $lanAddress
```

例如：

```powershell
py -m http.server 8000 --bind 192.168.2.152
```

电脑浏览器可以访问同一个局域网地址。为了避免创建另一套数据，不要一会儿使用 `localhost`，一会儿使用 WLAN IP。

### 更安全的独立运行目录

Python 简易服务器会提供启动目录中的文件。在局域网长期开放时，建议只复制运行所需的五个静态文件：

```powershell
$serveRoot = Join-Path $env:TEMP "personal-ai-os-static"
New-Item -ItemType Directory -Force -Path $serveRoot
Copy-Item -LiteralPath index.html,style.css,storage.js,memory.js,app.js -Destination $serveRoot -Force
$lanAddress = "192.168.2.152" # 替换为电脑实际的 WLAN IPv4 地址
py -m http.server 8000 --bind $lanAddress --directory $serveRoot
```

这样不会通过静态服务器暴露 `.git`、测试文件或项目文档。代码升级后需要重新复制这五个文件。

如果系统只提供 `python` 命令，将上述 `py` 替换为 `python`。

## 5. 手机通过局域网访问

1. 确认手机和电脑连接同一个 Wi-Fi。
2. 确认 Windows 将该 Wi-Fi 设置为“专用网络”。
3. 启动本地 HTTP 服务，并保持终端窗口打开。
4. 在手机浏览器输入 `http://<电脑 WLAN IPv4 地址>:8000/`。
5. 创建测试记录，刷新页面确认数据保留。

手机记录保存在手机浏览器中。电脑只是提供静态页面文件，不会收到或保存手机的业务数据。

安全要求：

- 只对 Windows 防火墙中的专用网络开放端口。
- 不要在酒店、机场等不可信 Wi-Fi 上开放。
- 不要配置路由器端口转发或公网暴露。
- 使用结束后按 `Ctrl+C` 停止服务。

## 6. 数据为什么会彼此隔离

浏览器按 origin 隔离 `localStorage`。origin 由协议、主机和端口共同决定；`file://` 还有浏览器特定行为。

例如，下面每个地址都是独立数据空间：

```text
file:///.../index.html
http://localhost:8000/
http://192.168.2.152:8000/
http://192.168.2.152:8080/
https://your-project.vercel.app/
```

因此：

- 手机和电脑不会自动同步。
- HTTP 和 HTTPS 数据不共享。
- 更换端口会出现一套空数据。
- 路由器给电脑分配新 IP 后，新地址也会出现一套空数据。
- 清除某个站点的浏览器数据会删除该 origin 下的记录。

建议为每台设备选择一个主要入口，并保持协议、主机和端口稳定。可以在路由器中为电脑设置 DHCP 地址保留，或使用能够稳定解析的局域网主机名。

## 7. JSON 备份策略

进入“个人记忆”，在导出前核对页面显示的记录数量，再选择“导出 JSON 备份”。文件名包含导出日期和时间。

导出包包含版本元信息、各集合统计以及完整原始 Schema v3 数据，不会删除字段、压缩隐藏字段或改写业务结构。导出过程不会修改 `localStorage`，也不会上传数据。

推荐节奏：

- 每周备份一次。
- 完成重要目标或项目阶段后备份。
- 清理浏览器数据、更换设备或重装系统前备份。
- 更换本地地址、端口、浏览器或 Vercel 域名前备份。
- 升级应用版本前备份。

至少保留两份副本，例如电脑本地磁盘和个人移动存储。若使用云盘，应由用户自行判断隐私边界并考虑加密。

Alpha 3.5.2 提供本地 JSON 恢复：在“个人记忆”中选择备份后，先核对数据来源、创建时间与六类记录数量，再明确确认恢复。恢复只允许在当前数据空间为空时执行；已有任何业务记录都会拒绝覆盖，也不会自动合并。解析或校验失败不会修改现有数据。

## 8. Vercel 与本地模式共存

Vercel 和本地模式运行同一套静态代码，但承担不同角色：

- 本地模式是无网络情况下的可靠核心入口。
- 局域网 HTTP 让手机在不经过外部网络的情况下加载工作台。
- Vercel 提供便捷的公网 HTTPS 页面入口。

Vercel 不保存业务数据。Vercel 域名下的数据仍在当前设备的浏览器 `localStorage` 中。由于当前没有 Service Worker，Vercel 资源不能保证断网后继续加载。

从 Vercel 切换到本地地址时，本地页面不会看到 Vercel origin 中的数据；反向切换同样如此。可以先在原入口导出备份，再在目标入口的空数据空间中恢复；不要假设不同入口已经自动同步。

## 9. 常见问题

### 手机无法打开页面

- 确认两台设备位于同一 Wi-Fi 和同一网段。
- 使用 WLAN IPv4 地址，不要使用 VPN、WARP、回环或虚拟网卡地址。
- 检查服务器终端是否仍在运行。
- 检查 Windows 防火墙是否允许 Python 在专用网络通信。
- 检查路由器是否启用了 AP 隔离或客户端隔离。
- 检查 URL 中的 IP 和端口是否与启动命令一致。

### 页面突然显示为空

- 先检查是否换了浏览器、协议、IP、端口或域名。
- 回到原来的完整地址查看原 origin 中的数据。
- 不要反复尝试不同地址后在多套数据之间继续录入。
- 找到正确数据后立即导出 JSON 备份。

### 端口 8000 被占用

可以选择其他端口，但新端口属于新的 origin，旧端口中的浏览器数据不会自动出现。若已有长期数据，应优先停止占用端口的旧服务并继续使用原端口。

## 10. 当前验证结论

- `file://` 模式可加载全部静态资源并持久化 Schema v3 数据。
- 核心运行文件没有外部 URL、网络请求或模型 API 调用。
- 现有自动测试覆盖记忆投影、记忆中心、备份格式和生命周期保留。
- 无 VPN、无外部网络、无 AI 服务时，核心功能仍可运行。
- Vercel 是可选分发方式，不是本地数据层或核心功能的依赖。
